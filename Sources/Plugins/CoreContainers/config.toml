abstract = "Core container and pod management plugin"
author = "Apple"
version = 0.1

[servicesConfig]
loadAtBoot = true
runAtLoad = false
defaultArguments = []

[[servicesConfig.services]]
type = "core"
description = "Provide an XPC interface to manage containers and the pods that machine them."
